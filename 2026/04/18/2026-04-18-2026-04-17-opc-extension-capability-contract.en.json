{
  "slug": "2026-04-18-2026-04-17-opc-extension-capability-contract",
  "lang": "en",
  "title": "Smoothing Out OPC Extensions: From Hardcoded meta.nodes to Capability Contracts",
  "summary": "The first version of OPC's extension system worked, but activation was hardcoded as `meta.nodes = [\"code-review\"]`—adding a new flow meant changing extension code, creating tight coupling. After rewriting it with capability contracts (provides × requires), extensions no longer know OPC node names. Also fixed 4 gotchas that would frustrate users.",
  "body": "## Background\n\nOPC (One-Person Company) is an agent framework I built—each flow is a chain of nodes, with subagents running between nodes, reviewing each other, and finally a judge scoring the results. I wanted to add an extension mechanism: let specialist tools like `design-lint` and `visual-eval` hook into specific nodes.\n\nThe first version shipped in 48 hours and worked. Then the metaphor judge gave \"non-hardcoded activation\" a 2/5. The architecture agent diagnosed further:\n\n> `meta.nodes = [\"code-review\", \"acceptance\"]` is hardcoded. Extensions know OPC node names. Adding a new flow or changing a node name means coming back to modify extension code. Tight coupling.\n\nMy first instinct was to argue—the `meta.nodes` array only has 3 elements, what's the big deal with changing it? But I knew the judge was right: **This isn't about the size of technical debt, it's about architectural direction.** Right now 3 nodes and 3 extensions barely work; with 10 flows and 5 extensions you get an N×M coupling matrix.\n\n## Redesign: Capability Contract\n\nInspiration came from Linux `/proc/capabilities`, SELinux capabilities, and TypeScript's structural typing. The core principle is simple:\n\n> Neither side knows the other directly—they only share a common vocabulary.\n\nApplied to OPC:\n\n- **Node side**: Flow template declares `nodeCapabilities[\"code-review\"] = [\"code-quality-check\", \"visual-consistency-check\"]`\n- **Extension side**: hook.mjs declares `meta.provides = [\"visual-consistency-check\"]`\n- **Harness side**: Triggers when `ext.meta.provides ∩ node.nodeCapabilities ≠ ∅`\n\nThe core matching logic is three lines of JS:\n\n```js\nconst requires = new Set(ctx.nodeCapabilities ?? []);\nconst provides = ext.meta.provides ?? [];\nconst fires = provides.some(p => requires.has(p));\n```\n\nExtensions no longer know any OPC node names. I can add a new node called `post-launch-sim`—as long as it declares `[\"user-simulation\"]`, the `visual-eval` extension automatically fires on it. No extension code changes required.\n\n## Governance Layer: CAPABILITIES.md\n\nCapability names are free-form strings. Runtime doesn't validate—typo one character (`vissual-consistency-check`) and it silently won't fire. That's the price of KISS.\n\nThe solution isn't adding validation, it's adding **governance docs**. I wrote `~/.opc/extensions/CAPABILITIES.md` with a canonical capability table: `code-quality-check`, `visual-consistency-check`, `user-simulation`, `security-check`, `a11y-check`. PRs that add new capabilities must first add a line to this table. Governance through convention, not code—just like HTTP status codes.\n\n## Smoothing: Four User-Breaking Gotchas\n\nWhile refactoring, I filled several holes the old version had been hiding. Each one looks like a small bug individually, but together they create the difference between \"works when you click\" and \"explodes when you click.\"\n\n### Gotcha 1: `.git` Directory Loaded as Extension\n\nExtensions live in `~/.opc/extensions/`, each subdirectory is an extension. My initial scan logic was:\n\n```js\nconst dirs = readdirSync(base);  // List all subdirectories\nfor (const d of dirs) { await loadExt(join(base, d)); }\n```\n\nThen I `git clone` this repo, and `.git/` ended up in the dirs array. `loadExt` reports \"can't find hook.mjs\", throws, harness FATAL.\n\nFix: Skip dotfiles and require `hook.mjs` to exist—two lines that should've been there from day one.\n\n### Gotcha 2: Hook Has No timeout\n\n`verdictAppend(ctx)` calls `execFileSync(ENGINE, ..., { timeout: 60_000 })`—the engine process has timeout, but **the hook function itself** (the `await` on child_process) has no timeout. If a hook author writes an infinite `await new Promise(() => {})`, the harness hangs forever.\n\nFix: Add a `withTimeout(promise, ms)` wrapper using `Promise.race`, timeout warns + continues. Default 60s, overridable with env var `OPC_HOOK_TIMEOUT_MS`.\n\n### Gotcha 3: Hook Returns Wrong Type and Crashes\n\n`promptAppend` expects to return `string`. Hook author accidentally `return 42`, or `return undefined`, or `return Promise`—harness directly calls `result.includes(...)` and crashes.\n\nFix: Type guards. Return non-string gets warned + treated as `\"\"`. Same for `verdictAppend`—must be array, elements must have `severity/category/message` fields. Legacy `{ emoji, text, file }` format gets auto-normalized with a one-time warning for authors to migrate.\n\n### Gotcha 4: Dual `normalizeHook` Implementation\n\nHook files can be written in three formats (default export + hooks object, named export + camelCase, named export + dot-notation key). All three must be supported (historical reasons). I had two normalize codebases: one in `extensions.mjs` for runtime, one in `extension-test` CLI for dev. The two got out of sync—hooks that passed CLI testing would explode at runtime.\n\nFix: Extract canonical `normalizeHook(raw, mod)`, `export` it, both CLI and runtime import this one version. Deleted 70 lines of duplicate code.\n\n## Verification: 40 Unit Tests + 2 Smoke Tests\n\nAfter all changes, ran tests: `node --test bin/lib/extensions.test.mjs` → 40 tests passed, 0 failed.\n\nCoverage:\n- `loadExtensions` various dir pollution / required vs optional / extensionOrder\n- Capability matching: match / no match / empty provides / empty requires / multiple\n- Timeout: slow hooks pass, bomb hooks get killed\n- Legacy format compatibility\n- Registry cache roundtrip\n- Benchmark bypass (`OPC_DISABLE_EXTENSIONS=1` / whitelist / priority)\n\nTwo smoke tests (real flows):\n1. `code-review` node (requires `[\"visual-consistency-check\"]`) → `design-lint` extension's prompt appears in subagent context\n2. `build` node (no `nodeCapabilities` declared) → no extensions fire, clean stdout\n\n## Reflection: Things That Should've Been Right in V1\n\n- **Activation is a contract, not a name list**. Any design where \"A knows B's name\" is a smell.\n- **Anywhere that can throw must warn + continue**. Extensions aren't core functionality—if they explode, they can't drag the whole run down.\n- **Type guards belong on the consumer side**. Hook authors making mistakes is normal—runtime catching them is my responsibility.\n- **Governance layer is independent of code layer**. Free-form capability names are a feature—`CAPABILITIES.md` is governance, not validation code.\n\nThe difference between an agent framework that \"works\" and one that's \"usable\" isn't features—it's **thoughtfulness toward users**. Every time a user gets frustrated, they use you one less time. These frameworks don't die from bugs, they die from \"I can't be bothered anymore.\"",
  "heroImage": "",
  "tags": [
    "opc",
    "extension-system",
    "capability-contract",
    "architecture",
    "agent-framework",
    "type-safety",
    "governance"
  ],
  "sessionId": "1854dbbd-13cd-4e6e-b298-628a27c8a1bf",
  "chunkIndices": [
    57,
    58,
    59,
    60,
    61,
    62,
    63,
    64,
    65,
    66,
    67,
    68,
    69,
    70,
    71,
    72,
    73,
    77,
    78,
    79,
    80,
    81
  ],
  "project": "opc",
  "date": "2026-04-18",
  "duration": "",
  "stats": {}
}